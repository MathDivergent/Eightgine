# [[Binaries]]
eightgine_add_module(MODULE_NAME "${EIGHTGINE_CORE_LIBRARY_NAME}"
    MODULE_SOURCES_DIR "${CMAKE_CURRENT_LIST_DIR}"
)

# [[Dependencies]]
eightgine_link_dependency(MODULE_OR_EXECUTABLE_NAME "${EIGHTGINE_CORE_LIBRARY_NAME}"
    DEPENDENCY_NAME "SDL2"
    DEPENDENCY_INCLUDE_DIR "${EIGHTGINE_THIRD_PARTY_DIR}/SDL2/include"
    DEPENDENCY_LIB_DIR "${EIGHTGINE_THIRD_PARTY_DIR}/SDL2/lib"
    DEPENDENCY_BIN_DIR "${EIGHTGINE_THIRD_PARTY_DIR}/SDL2/bin"
)

# [[Binaries]]
eightgine_add_module(MODULE_NAME "Game"
    MODULE_TYPE SHARED
    MODULE_INCLUDE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/include"
    MODULE_SOURCES_DIR "${CMAKE_CURRENT_SOURCE_DIR}/src" "${CMAKE_CURRENT_SOURCE_DIR}/include"
    MODULE_DESTINATION_DIR "${PROJECT_PLUGINS_DIR}"
)

# [[Dependencies]]
eightgine_add_dependency(MODULE_OR_EXECUTABLE_NAME "Game" DEPENDENCY_NAME "EightgineCore")

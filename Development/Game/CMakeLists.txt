# [[Binaries]]
eightgine_add_module(MODULE_NAME "${PROJECT_GAME_LIBRARY_NAME}"
    MODULE_SOURCES_DIR "${CMAKE_CURRENT_LIST_DIR}"
)

# [[Dependencies]]
eightgine_link_dependency(MODULE_OR_EXECUTABLE_NAME "${PROJECT_GAME_LIBRARY_NAME}"
    DEPENDENCY_NAME "${EIGHTGINE_CORE_LIBRARY_NAME}"
    DEPENDENCY_INCLUDE_DIR "${EIGHTGINE_CORE_LIBRARY_INCLUDE_DIR}"
)

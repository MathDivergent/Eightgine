![language](https://img.shields.io/badge/language-C%2B%2B-blue)
![OS](https://img.shields.io/badge/OS-linux,%20windows,%20mac-red)
![tests](https://img.shields.io/badge/tests-automated-yellow)
[![Free](https://img.shields.io/badge/MIT-license-green)](https://github.com/svatostop/anthrax-ai?tab=MIT-1-ov-file)

TODO: update structure of engine, project
TODO: git config core.ignorecase false

Структура папок движка:
- Eightgine - Содержит 3 ключевые папки.
    - Binaries - Содержит исполняемые файлы и шаред библиотеки. Исполняемый файл, сгенерированный сборкой в зависимости от опции WITH_EDITOR, будет запускать пустой проект с редактором и без него.
    - Development - Содержит файлы необходимые для разработки проекта. Содержит шаблон CMakeLists.txt файла для сборки проекта.
        - Engine - Содержит исходный код движка и редактора.
        - Game - Содержит шаблон исходного кода для сборки исполняемого файла проекта.
        - ThirdParty - Содержит исходные файлы сторонних библиотек, фреймворков, необходимых для движка и редактора.
        - Intermediate - Содержит артефакты сборки и промежуточные файлы. Может быть удалена.
    - Resources - Содержит сереализированные данные, ассеты редактора.

Структура папок проекта:
- Project - Содержит 3 ключевые папки. Может иметь произвольное имя.
    - Binaries - Содержит исполняемые файлы и шаред библиотеки. Исполняемый файл, сгенерированный сборкой в зависимости от опции WITH_EDITOR, будет запускать проект с редактором и без него.
    - Development - Содержит файлы необходимые для разработки проекта. Содержит копию шаблона CMakeLists.txt файла для сборки проекта. Может изменятся разработчиком.
        - Game - Содержит исходный код проекта. Содержи копию шаблона исходного кода для сборки исполняемого файлы проекта.
        - ThirdParty - Содержит исходные файлы сторонних библиотек, фреймворков, необходимых для проекта.
    - Resources - Содержит сереализированные данные, ассеты проекта.

Структура папок билда:
- Project - Содержит 2 ключевые папки. Может иметь произвольное имя.
    - Binaries - Содержит исполняемые файлы и шаред библиотеки.
    - Resources - Содержит сереализированные данные, ассеты проекта.

Замечания:
- Binaries - Кастомные библиотеки, добавленные в CMakeLists.txt, будут автоматически копироваться.
- Development - Может быть удалена для сборки с включенной опцией WITH_EDITOR.

Для сборки под Qt Creator (одно из двух):
- Необходимо для каждой конфигурации указывать путь: "Intermediate/BUILD_TYPE". 
- Зайти в Edit -> Preferences -> Build & Run -> Default Build Properties и установить поле Default build directorty в "./Intermediate/%{BuildConfig:Name}".

Для сборки под CLion:
- Необходимо для каждой конфигурации указывать путь: "Intermediate/BUILD_TYPE".


Архитектура движка:
- Core - Включает базовые элементы движка, которые используются в других подсистемах, а также в самой игре.
    - CMain - Описывает логику работы главного цикла движка.
    - CTime - Позволяет получать базовую временную информацию, настраивать частоту кадров и прочее.
    - CVector - Математическая единица описывающая смещение, масштаирование, поворот обьекта в пространстве.
    - CObject - Минимальная композитивная единица в пространстве.
    - CSpace - Является пространством игры, состоит из миров. Обрабатываются все активные миры.
    - CWorld - Является контейнером для локаций. Может быть в множественном количестве, а также в активированным и деактивированным состоянии. Обрабатывает все активные локации.
    - CLocation - Является базовой единицой для хранения игровых обьектов, включает вьюпорт. Может быть в множественом количестве, а также в активированным и деактивированном состоянии. Обрабатыает все активные игровые обьекты.
    - CViewport - Отображает всё, что видит камера, тоесть отвечает за то, где мы видим камерой. Может быть в множественном количестве, тоесть пара вьюпорт-камера.
    - CCamera - Определяет положение камеры, проекцию и перспективу, тоесть отвечает за то, как мы видим камерой.
    - CWindow - Является контейнером отображаемых вьюпортов. Может быть в множественном количестве.
    - CTransform - Математическая единица обьеденяющая положение, попорот, масштаб обьекта.
    - SPosition - Математическая единица отвечающая за положение обьекта в пространстве.
    - SRotation - Математическая единица отвечающая за поорот обьекта.
    - SScale - Математическая единица отвечающая за масштабирование обьекта.
    - CPolygon - Замкнутая цепочка точек, описывающая форму обьекта.
    - CMath - Состоит из набора математических функций, алгоритмов и операций, работающих с математическими единицами.
    - CResourceManager - Отвечает за подгрузку игровых ресурсов, распределением между мирами, локациями.
    - CEventManager - Обрабатывает все входящие события, такие как ввод, внутридвижковые и внутриигровые события.
    - CMemoryManager - Отвечает за управление памятью процесса.
    - CReflection - Отвечает за отражение кода. Используется для метаинформации, сериализации, редактора, консоли.
    - CSerialization - Отвечает за сериализацию/десериализацию данныx. Используется для хранения данных, для сетевых пакетов, стриминга.
    - CObjectSchema - Является сериализируемым обьектом. Используется для разметки обьекта, передачи по сети.
    - CWorkerThread - Предоставляет функциональность потока выполнения.
    - CThreadTask - Задача для пулового потока. Может быть в множественном количестве.
    - CThreadPoolManager - Управляет пулами потоков. Предоставляет возможность для управления потоками, приоритетом задач.
    - CPlatform - Предназначен для использования платформенно-зависимого функционала.

- Renderer - Содержит классы для работы с графикой.
    - RTexture - Обёртка над сырой текстурой. Предоставляет функциональность для настраивания текстур.
    - RSprite - Определяет визуальную составляющую обьекта в пространстве.
    - RSpriteComposite - Описывает стопку спрайтов для рисования. Используется для контроля видимости.
    - RRenderTarget - Описывает холст рисования. 
    - RPostProcessManager -	Предоставляет функциональность для работы с готовым кадром. Используется для цветокорекции, сглаживания, размытия.
    - RRendererManager - Управляет рисованием окон и холстов.

- Audio - Содержит классы для работы с аудио.
    - AAudio - Обёртка над сырым аудио. Предоставляет функциональность для настраивания аудио.
    - ASound - Описывает звук в пространстве.
    - AMusic - Описывает музыку в пространстве.
    - AAudioManager - Управляет активированными аудио.

- Input - Содержит классы для обработки перефирийных событий. 
    - IMouseInput - Обёртка для обработки событий ввода мышки.
    - IKeyboardInput - Обёртка для обработки событий ввода клавиатуры.
    - IJoystickInput - Обёртка для обработки событий ввода джойстика.
    - IInputManager - Управляет активированными вводами.

- Physics - Содержит классы для программирования физики.
    - PMovementPhysics - Физика движущихся обьектов.
    - PDynamicPhysics - Физика динамических обьектов.
    - PCollider - Описывает замкнутую форму в пространстве.
    - PRayCast - Описывает лучь (линию) в пространстве.

- Scripting - Содержит классы для программирования состояния, поведения, скриптов. 
    - SStateMachine - Описывает стековый конечный автомат.
    - SExecutionTree - Описывает дерево выполнения. Имеет схожую функциональность с деревом поведения.
    - SScriptableObject - Предоставляет функциональность для скриптования обьектов с помощью Lua, ChaiScript, Python и других.
    - SScriptingContext - Описывает контекст скриптуемого обьекта.

- Objects - Содержит классы для программирования и разработки игровых обьектов.
    - OObjectComponent - Описывает компонент обьекта.
    - OStaticObject - Описывает статический обьект в пространстве.
    - OKinematicObject - Описывает управляемый обьект в пространстве.
    - ODynamicObject - Описывает динамический обьект в пространстве.

- Lighting - Содержит классы для программирования и разработки освещения- 
    - LStaticLight - Описывает статический источник света.
    - LDynamicLight - Описывает динамический источник света.
    - LShadow - Описывает тень в пространстве.
    - LLightingEditor - Предоставляет функциональность для создания освещения.
    - LShadowingEditor - Предоставляет функциональность для создания теней.

- Animations - Содержит классы для программирования и разработки анимаций.
    - ABaseAnimation - Предоставляет базовую функциональность для анимации.
    - ASpriteAnimation - Описывает спрайтовую анимацию.
    - ASkeletalAnimation - Описывает скелетную анимацию.
    - AAnimationEditor - Предоставляет функциональность для создания анимаций.

- Particles - Содержит классы для программирования и разработки частиц- 
    - PParticlesBase - Предоставляет базовую функциональность для определения частиц.
    - PParticlesEditor - Предоставляет функциональность для создания частиц.

- Network - Содержит классы для сетевого программирования.
    - NConnectionBase - Предоставляет базовую функциональность для сетевого программирования.
    - NServer - Предоставляет функциональность для серверной части.
    - NClient - Предоставляет функциональность для клиентской части.
    - NRemoteProcedureCall - Предоставляет функциональность для удалённого вызова.
    - NNetworkManager - Управляет сетевым взаимодействием.

- GUI
    - GBase - Описывает графический обьект интерфейса.
    - GButton - Описывает нажымаемый обьект.
    - GSliderBlock - Описывает прокручиваемый блок.
    - GSliderBar - Описывает прокручиваемый ползунок.
    - GCheckButton?
    - GRadioButton?
    - GBox?
    - GLayout?
    - GTextBox - Описывает текстовый обьект.
    - GTextField - Описывает обьект тектового ввода.
    - GPanel?
    - GImage - Описывает отрисовывающийся обьект.

- Editor
    - EDebugDrawUtility - Предоставляет функциональность для отрисовки елементов дебага.
    - EDetailsPanel - Панель включающая свойства и компоненты обьектов.
    - EObjectHierarchy - Описывает иерархию обьектов в пространстве.
    - EResourcesBrowser - Предоставляет функциональность для структурирования и отображения ресурсов.
    - ELocationEditor - Предоставляет функциональность для редактирования локации.


Прежде чем спроектировать главный цикл движка стоит обсудить компоненты, которые будет включать любая 2D игра:
1. Спрайты - Весь визуал игры строится на спрайтах. С их помощью, мы можем реализовать графический интерфейс, игровое окружение, анимации и т.п;
2. Звук - Является вторым, после визуального восприятия, элементом игрового опыта. С его помощью, мы можем реализовать динамику, отзывчивость, сопровождение и т.д.
3. Ввод - Благодаря возможности ввода, мы можем контролировать игровой процес, управлять персонажами, взаимодействовать с игрой и т.п;
4. Игровые ресурсы - В общем смысле, включат картинки, звук и другие файлы, поддающиеся кастомизации. Благодаря доступу к ресурсам игры, открывается возможность для создания модов и дополнений;
5. Игровая логика - Самое важное, что связывает остальные 4 компоненты, благодаря ей строится весь игровой процес.

Имея полное представление об компонентах, можно распределить их обработку так, чтобы они не мешали друг другу и могли свободно масштабироваться. Первым делом, после запуска игры может произойти одна из 3-х вещей:
1. Экран загрузки игровых ресурсов (и загрузка игровых ресурсов) -> отображение игрового меню;
2. Экран со вступительными картинками и видео (и загрузка игровых ресурсов) -> отображение игрового меню;
3. Отображение игрового меню (и загрузка игровых ресурсов).

Вышеперечисленные вещи могут комбинироваться, но унифицировать поведение в рамках движка нельзя, поэтому предлагается гибкий подход. Базовая архитектура обьектов движка состоит с пространства и миров, где миры в свою очередь включат в себя локации. Представим, что первым миром в игре будет мир с загрузочным экраном, или же локация. Загрузка игровых ресурсов может выполнятся динамически, во время игры, например, при загрузке пространства, мира, локации. И пространство, и мир, и локация имеют ресурсы, необходимые для их выполнения. Ресурсы, распространяющиеся на несколько миров, хранятся единичным экземпляром в пространстве. Ресурсы, распространяющиеся на несколько локаций, хранятся в единичном экземпляре в мире. Любой активированный мир или локация автоматически подтягивает загрузку ресурсов, при этом не исключается распространение подгрузки ресурсов через связи. Например, если А связан с Б, а Б связан с В, то при загрузке А, можно дополнительно подгрузить Б или даже В.
Иходя из этого, в первую очередь, при запуске игры создается пространство, после которого с конфигурационного файла условно загружается мир, в чьих правах контролировать процес загрузки ресурсов и локаций. Локация сама по себе является полноценной игровой площадкой, и следовательно для игры с открытым миром будет достаточно 2-3 локации (одна для главного меню, вторая для игрового уровня, третья в качестве вспомогательной).
Таким образом, в главном цикле движка на первой позиции стоит инициализация корневых подсистем (память, файлы, потоки), после которой уже идёт загрузка конфигурационного файла, с которого начинается инициализация базовых подсистем (отрисовка, звук, ввод, сеть), а затем загрука мира. Далее по ходу выполнения подгружаются остальные подсистемы (физика, анимации, освещение, скриптование). Но поскольку корневые подсистемы также требуют конфигурационных данных, их передача выполняется средставами системы сборки. Учитывая, что при сборке игры требуется также собирать движок, эти конфигурационные данные могут регламентироваться самим разработчиком, если значения по умолчанию его не удовлетворяют.

Когда загрузка и активация мира выполнена со всеми нужными подсистемами, главный цикл движка начинает обработку активированного мира и его локакий. Активацию и деактивацию разрешено выполнять в главном цикле, которая применяется на следующем тике. Заметим, что подсистемы ввода, отрисовки, звука, файлов, сети, игрового логики работают паралельно. Тоесть, когда выполняется загрузка уровня, мы можем спокойно можем посылать ввод, отрисовывать окно загруки, проигрывать звук, или вообще подгружать соседние локации. Поскольку корневая подсистема потоков хранит резервные потоки, их можно использовать для собственных целей, если это необходимо.

Порядок обновления базовых подсистем должен выполняется в строгом порядке, но чтобы понять, что за чем следует, разобраться с предназначением подсистем:
1. Ввода - В любой момент времени, игрок может посылать события с устройств ввода, например: мышка, клавиатура, джойстик. Но обрабатывать события ввода мы можем только на фиксированном временном шаге (тике). Следовательно, чтобы игра была отзывчивой, стоит обрабатывать ввод в первую очередь.
2. Игровая логика - Отвечает за поведение игровых обьектов, в зависимости от ввода игрока и других событий. Всегда работает на серверной стороне. В случае одиночной игры сервером выступает устройство, на которой запущена игра.
3. Звук - Воспроизводит звуковую составляющую. Если мы хотим всегда получать актуальное воспроизведение, необходимо обновлять после его обработки игровой логики. 
4. Графика - Отображает визуальную составляющую. Если мы хотим всегда получать актуальное отображение, необходимо обновлять его после воспроизведение звука (из-за буферизации аудио, которая имеет задержки).

Отметим, что игровая логика выполняется только на сервере, который принимает данные от клиентов, обрабатывает их и отправляет. Каждый клиент получает данные на основании которых проигрывается звук, отрисовывается графика. Архитектурно клиент имеет буфер для отправки данные на сервер, а также буфер для их получения. Сервер при это хранит буфера для получения данные от клиентов, а также для отправки. Эти буфера хранятся в подсистеме Сети.

Правильным и универсальным способом, будет загрузка модулей/плагинов через конфиг файлы, в котором мы контролируем, что загружать и в каком порядке. Загрузка модулей контролируется в конфигах, и запекается при сборке не WITH_EDITOR.


===УДАЛИТЬ===
Сначала реализовываем графический интерфейс, который включает в себя следующие компоненты:
1. Transform -
2. Collider -
3. Renderer -
4. Positioner -
5. Button, CheckButton, RadioButton -
6. CheckButtonGroup, RadioButtonGroup -
7. Slider, SliderBar -
8. Form - 
9. TabForm, TabGroup -
10. ScrollForm, ScrollButton -
11. TextBox - 
12. TextField -
13. Image -
14. Droddown -
15. Hint -

===

[] В целом нет необходимости усложнять архмтектуру лишними классами или компонентами, если можно сделать таким образом, чтобы вместо жёстко связанной логики было легко адаптируемые обьекты. Для этого предлагается контролировать UI через отрисовку поверх, но двигать при этом обьекты с камерой (вернее подвязать на событие движения камеры). Почему такой подход - потому что это позволит сделать динамичность элементов интерфейса, без необходимости переподключать иерархию и т.д. Все выше компоненты будут быть как обьекты мира, хранящихся в пресохраненных обьектах (ObjectTemplate/ObjectModel) и быть как Behavior- обьекты поведения. Component-ы при этом содержат данные например.

[×] Transform - структура, хранящая Position, Rotation, Scale структуры, производные от Vector2 / Vector3, где значения сохраняются в локальном пространстве. Используется для определения объектов в пространстве.

[×] Collider - структура, хранящая Polygon2 / Polygon3, где значения сохраняются в локальном пространстве. Используется для 

взаимодействия обьектов друг с другом, а также обьектов с точками.

[] Mask - структура, хранящая Collider стуруктуры (входное значение и кеш), где значения сохраняются в локальном пространстве. Используется для корректировки визуализации объектов.

[×] Renderer - структура, хранящая стек спрайтов. Используется для визуализации объектов в пространстве.

[×] ObjectTemplate<> - обьект, являющийся образцом для наполнения.

[×] ObjectModel<> - обьект, являющийся образцом для клонирования.

[×] ObjectTemplate Button - образец кнопки, требует наличие трансформы, колайдера, отрисовщика (спрайт кнопки в активном / неактивном состоянии и спрайт кнопки в нажатом состоянии). Включает защищённые делегаты на нажатие миши (OnMouseDown, OnMouseHold, OnMouseUp), публичные делегаты на нажатие (OnClick).

[×] ObjectTemplate SliderBlock - образец слайдера, требует наличие трансформы, отрисовщика (спрайт слайдер поля в активном / не активном состоянии), слайдер линию (две трансформы / точки). Включает SliderBar, публичный делегаты на обновление слайдера (OnValueWrote, OnValueChanged).

[×] ObjectTemplate SliderBar - образец ползунка слайдера, требует наличие трансформы, колайдера, отрисовщика (спрайт ползунка слайдера в активном / не активном состоянии и нажатом состоянии). Включает защищенные делегаты на нажатие и движения мыши (OnMouseDown, OnMouseHold, OnMouseMove, OnMouseUp), публичные делегаты на нажатие и движение мыши (OnClick, OnMove).

[×] ObjectTemplate CheckButton - образец флага, требует наличие трансформы, колайдера, отрисовщика (спрайт флага в активном /не активном состоянии, спрайт галочки в активном / не активном состоянии). Включает публичные делегаты на обновление флага (OnEnabled, OnDisabled, OnSwitched).

[×] ObjectTemplate RadioButton - образец переключателя, требует наличие трансформы, колайдера, отрисовщика (спрайт кнопки в активном / не активном состоянии, спрайт галочки в активном / не активном состоянии), RadioButtonGroup (контейнер переключателей). Включает защищённые делегаты на нажатие мыши (OnMouseDown, OnMouseHold, OnMouseUp), публичный делегат на нажатие мыши (OnClick), публичные делегаты на обновление переключателя (OnEnabled, OnDisabled, OnSwitched).

[×] ObjectTemplate Block - образец блока / формы (контейнера для других элементов), требует наличие трансформы, колайдера, отрисовщика (спрайт блока / формы в активном (или сфокусированном) / не активном (или расфокусированном) состоянии), маски (опционально), слайдера (опционально). Включает защищенные делегаты на нажатие и движение мыши (OnMouseDown, OnMouseHold, OnMouseMove, OnMouseUp, OnMouseScroll), публичный делегат на нажатие и движении мыши (OnFocus, OnUnfocus, OnClick, OnMove, OnScroll).

[×] ObjectTemplate TextBox - образец текстового блока, требует наличие трансформы, шрифта.

[×] ObjectTemplate TextField - образец текстового поля, требует наличие трансформы, колайдера, отрисовщика (спрайт текстового поля, спрайт курсора), шрифт.

===

[] Аудио может проигрываться в двух режимах:

[] Положение звука в экранных координатах слушателя - легче для восприятия, звук исходит относительно игрока (зрителя).

[] Положение звука в мировых координатах слушателя - труднее для восприятия, звук исходит относительно персонажа (слушателя).

[] Вообще думаю, что можно идти на компромис между сжатием при варки билда, и прямым использованием (без сжатия), добавлю просто в настройки движка такую опцию для сборки.

[] ObjectTemplate Sound - образец звука, требует наличие трансформы, колайдера (круг, полигон), аудио (конфигурации).

[] ObjectTemplate Music - образец музыки, требует наличие трансформы, колайдера (круг, полигон), аудио (конфигурации).

[] Концепция связанная с аналогией в космосе. Главный обьект (инстанс приложение) - это Space. Основной составной единицей является World (мир), который состоит из Location (уровней). Локация является минимальной составной единицей, в которой сохраняются обьекты (суб составные единицы). Чтобы было проще понять, вот пример: При запуске движка, создается пространство, пространство обязано быть заполнено чем-то. И этим чем-то является по умолчанию движковый мир, в котором есть куча локаций (разные менюшки, панельки, окна). Учитывая, что в пространстве, каждый действующий мир обрабатывается, а в каждом мире обрабатывается каждая действующая локация, и при этом каждая локация может иметь свой вьюпорт - мы имеем, чёткую иерархию, позволяющую компоновать миры и локации в пронстранстве в пределах одного, или нескольких окон (окно не стоит принимать, чем-то особенным). Таким образом, в пространстве позволено динамически создавать/менять новые/старые миры и локации. Даже движковый мир может меняться. Запуская движок действующий миров будем как минимум один (движковый мир создается автоматически и устанавливается как стартовый). А запуская игру действующий мир будет тот, который сконфигурирован ранее как стартовый. Таким образом не только в движке может быть несколько действующих миров (движковый и игровые), а и в самой игре. Окно само по себе не является вьюпортом, а лишь холстом, на котором размещаются вьюпорты. Если одного окна не достаточно, можно создавать дополнительные окна и размещать в них остальные вьюпорты. Вьюпорт может отображаться внутри другого вьюпорта, путем отрисовки поверх него или накладываться, если альфа канал позволяет.


[LINUX]
; Build and setup 'fresh' gcc
; Use mirro when low download speed:
; https://ftp.sotirov-bg.net/pub/mirrors/gnu/gcc/gcc-13.2.0/gcc-13.2.0.tar.gz
sudo apt update && sudo apt install -y cifs-utils p7zip-full build-essential libwayland-dev libasound2-dev libpulse-dev libgmp-dev libmpfr-dev libmpc-dev flex bison texinfo zlib1g-dev curl cmake git libx11-dev libxext-dev libxrandr-dev libxinerama-dev libxcursor-dev libxfixes-dev libxi-dev && cd /tmp && curl -LO https://ftp.gnu.org/gnu/gcc/gcc-13.2.0/gcc-13.2.0.tar.gz && tar -xf gcc-13.2.0.tar.gz && cd gcc-13.2.0 && ./contrib/download_prerequisites && mkdir ../gcc-build && cd ../gcc-build && ../gcc-13.2.0/configure --enable-languages=c,c++ --disable-multilib --prefix=/opt/gcc-13.2.0 && make -j$(nproc) && sudo make install && sudo update-alternatives --install /usr/bin/gcc gcc /opt/gcc-13.2.0/bin/gcc 100 && sudo update-alternatives --install /usr/bin/g++ g++ /opt/gcc-13.2.0/bin/g++ 100 && sudo update-alternatives --config gcc && sudo update-alternatives --config g++


; Setup Debian (Use)
sudo apt purge -y 'gnome*' gdm3 task-gnome-desktop task-desktop gnome-shell mutter nautilus yelp gedit gnome-terminal && sudo apt autoremove --purge -y && sudo apt install -y cifs-utils p7zip-full sway xwayland


; Setup Fedora (Use)
sudo dnf install p7zip p7zip-plugins


; Setup Ubuntu (Dev)
sudo add-apt-repository ppa:ubuntu-toolchain-r/test && sudo apt update && sudo apt install -y cifs-utils p7zip-full build-essential libwayland-dev libegl1-mesa-dev libasound2-dev libpulse-dev libx11-dev libxext-dev libxrandr-dev libxinerama-dev libxcursor-dev libxfixes-dev libxi-dev git gcc-9 g++-9 && sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-9 90 && sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-9 90 && sudo update-alternatives --config gcc && sudo update-alternatives --config g++ && sudo snap install cmake --classic


; Download project
cd /home/user/Documents/ && git clone https://github.com/Sigma-Ryden/Eightgine


; Regenerate and Rebuild project
export Eightgine=/home/user/Documents/Eightgine && cd ${Eightgine}/Development && rm -rf Intermediate && mkdir Intermediate && cd Intermediate && /snap/bin/cmake .. && /snap/bin/cmake --build . --parallel


; Build project
cd /home/user/Documents/Eightgine/Development/Intermediate && /snap/bin/cmake --build . --parallel


; Run project
cd /home/user/Documents/Eightgine/BinariesWithEditor && ./Launcher


; Run project (No sound)
export SDL_AUDIODRIVER=dummy && cd /home/user/Documents/Eightgine/BinariesWithEditor && ./Launcher


; Package project
7z a -t7z /home/user/Documents/Eightgine.7z /home/user/Documents/Eightgine/ -xr\!.git -xr\!.gitignore -xr\!Development -xr\!README.md 


; Upload to Shared folder
sudo mkdir -p /mnt/Shared && sudo mount -t cifs //192.168.0.102/Shared /mnt/Shared -o username=user,password=0000 && cp /home/user/Documents/Eightgine.7z /mnt/Shared/ && sudo umount /mnt/Shared


; Download from Shared foler
sudo mkdir -p /mnt/Shared && sudo mount -t cifs //192.168.0.102/Shared /mnt/Shared -o username=user,password=0000 && rm -rf /home/user/Documents/Eightgine && 7z x /mnt/Shared/Eightgine.7z -o/home/user/Documents/ && sudo umount /mnt/Shared